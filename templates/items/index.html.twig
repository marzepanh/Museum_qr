{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
    <!-- Page content-->
    <div class="container mt-5">
        <div class="row">
            <div class="col-lg-8">
                <!-- Post content-->
                <article>
                    <!-- Post header-->
                    <header class="mb-4">
                        <!-- Post title-->
                        <h1 class="fw-bolder mb-1">{{ item.name }}</h1>
                        <!-- Post meta content
                        <a class="badge bg-secondary text-decoration-none link-light" href="#!">Web Design</a>
                        <a class="badge bg-secondary text-decoration-none link-light" href="#!">Freebies</a>
                        -->
                    </header>
                    <!-- Preview image figure-->
{#                    <figure class="mb-4"><img class="img-fluid" src="{{ asset('uploads/' ~ item.image) }}" width="350" height="100%" alt=""></figure>
                    <!-- Post content-->#}
                    <div class="row">
                        <div class="col-lg-6">
                            <figure class="mb-4"><img class="img-fluid" src="{{ asset('uploads/' ~ item.image) }}" width="350" height="100%" alt=""></figure>
                        </div>
                        <div class="col-lg-6">
                            <section class="mt-5">
                                    {% apply markdown_to_html %}
                                        {{ item.description }}
                                    {% endapply %}
                            </section>
                        </div>
                    </div>

                </article>
            </div>

{#            <div class="col-lg-4">

                <!-- Side widget-->
                <section class="mb-5">
                    <p class="fs-5 mb-4">{{ item.description }}</p>
                </section>
            </div>#}
        </div>
    </div>
{% endblock %}
